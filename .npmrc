# pnpm configuration for Radio-Inventar Monorepo

# Hoist Prisma packages to root node_modules so @prisma/client
# can find the generated client at node_modules/.prisma/client
# This is required because Prisma generates types to .prisma/client
# and expects @prisma/client to be in the same node_modules tree
public-hoist-pattern[]=@prisma/client
public-hoist-pattern[]=.prisma
